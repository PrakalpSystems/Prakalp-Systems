{
  "name": "my-static-site",
  "version": "1.0.0",
  "description": "A simple static website",
  "scripts": {
  "start": npx serve public/ -p $PORT
}
  },
  "dependencies": {
    "serve": "^14.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
