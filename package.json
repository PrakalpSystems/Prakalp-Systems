{
  "name": "my-static-site",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build step skipped'",
    "start": "npx serve . -p $PORT"
  },
  "dependencies": {
    "serve": "^14.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
{
  "scripts": {
    "start": "npx serve . -p $PORT"
  }
}
